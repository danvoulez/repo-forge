Goal: ship a concrete improvement for **this repository** using its real toolchain.

Load skills: `repo-worker`, `autonomous-review`, `sdk-reader` (only if touching Claude Agent SDK).

Spawn Task reviewers when the change is non-trivial.
